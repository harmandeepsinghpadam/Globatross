'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
//创建map类型数据
var createMap = exports.createMap = function createMap(arr) {
    var type = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'id';

    var Maps = new Map();
    var _iteratorNormalCompletion = true;
    var _didIteratorError = false;
    var _iteratorError = undefined;

    try {
        for (var _iterator = arr.keys()[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
            var i = _step.value;

            if (type) {
                Maps.set(arr[i][type], arr[i]);
            } else {
                Maps.set(i, arr[i]);
            }
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally {
        try {
            if (!_iteratorNormalCompletion && _iterator.return) {
                _iterator.return();
            }
        } finally {
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }

    return Maps;
};